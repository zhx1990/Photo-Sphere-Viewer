{
  "name": "@photo-sphere-viewer/resolution-plugin",
  "version": "0.0.0",
  "description": "Photo Sphere Viewer plugin that adds a setting to choose between multiple resolutions of the panorama.",
  "homepage": "https://photo-sphere-viewer.js.org/plugins/resolution",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@photo-sphere-viewer/core": "0.0.0",
    "@photo-sphere-viewer/settings-plugin": "0.0.0"
  },
  "scripts": {
    "build": "tsup",
    "watch": "tsup --watch",
    "lint": "eslint . --fix",
    "publish-dist": "cd dist && npm publish --tag=$NPM_TAG --access=public"
  },
  "psv": {
    "globalName": "PhotoSphereViewer.ResolutionPlugin"
  },
  "typedoc": {
    "displayName": "plugin: Resolution",
    "readmeFile": "./.typedoc/README.md"
  }
}
